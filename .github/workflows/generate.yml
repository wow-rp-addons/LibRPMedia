---
name: Generate Media
on:
  workflow_dispatch:
    inputs:
      standard_product:
        description: 'Standard product branch'
        required: true
        default: 'wow'
        type: choice
        options:
          - wow
          - wowt
          - wowxptr
          - wow_beta

      forever_product:
        description: 'Forever product branch'
        required: true
        default: 'wow_forever'
        type: choice
        options:
          - wow_forever
          - wow_classic_beta

      classic_product:
        description: 'Classic product branch'
        required: true
        default: 'wow_classic'
        type: choice
        options:
          - wow_classic
          - wow_classic_ptr
          - wow_classic_beta

      classic_vanilla_product:
        description: 'Vanilla product branch'
        required: true
        default: 'wow_classic_era'
        type: choice
        options:
          - wow_classic_era
          - wow_classic_era_ptr

      classic_tbc_product:
        description: 'TBC product branch'
        required: true
        default: 'wow_anniversary'
        type: choice
        options:
          - wow_anniversary
          - wow_anniversary_ptr

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -qqy ffmpeg libreadline-dev libsqlite3-dev
          sudo python3 -m pip install git+https://github.com/luarocks/hererocks
          sudo hererocks -l 5.1 -r latest /usr/local
          sudo luarocks make

      - name: Cache database
        uses: actions/cache@v3
        with:
          key: database
          path: |
            .cache/build.*
            .cache/cdn.*
            .cache/encoding.*
            .cache/index.*
            .cache/pconf.*
            .cache/root.*

      - name: Generate Media
        run: make ${STANDARD_PRODUCT} ${FOREVER_PRODUCT} ${CLASSIC_PRODUCT} ${CLASSIC_VANILLA_PRODUCT} ${CLASSIC_TBC_PRODUCT}
        env:
          STANDARD_PRODUCT: ${{ inputs.standard_product }}
          FOREVER_PRODUCT: ${{ inputs.forever_product }}
          CLASSIC_PRODUCT: ${{ inputs.classic_product }}
          CLASSIC_VANILLA_PRODUCT: ${{ inputs.classic_vanilla_product }}
          CLASSIC_TBC_PRODUCT: ${{ inputs.classic_tbc_product }}

      - name: Increment library version
        run: |
          if [[ -n "$(git status --porcelain=v1 2>/dev/null)" ]]; then
            perl -i -pe 's/(MINOR_VERSION = )(\d+)/$1.($2+1)/e' LibRPMedia-1.2.lua
          fi

      - name: Create Pull Request
        uses: peter-evans/create-pull-request@v6
        with:
          branch: actions/regenerate-databases
          commit-message: Regenerate Databases
          delete-branch: true
          title: Automated regeneration of media databases
          body: Automated regeneration of media databases.
          author: GitHub <noreply@github.com>
          reviewers: Meorawr
